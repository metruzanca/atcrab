# Repository Rules
- Keep things simple and avoid hasty abstractions.
- When committing use conventional commits.
- This is a library, don't panic instead return errors so the client can handle the errors
